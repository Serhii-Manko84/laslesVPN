const slider = document.querySelector(".customer-list");
const slides = slider.querySelectorAll(".customer-item");
const prevSlide = document.getElementById("prev");
const nextSlide = document.getElementById("next");

let currentIndex = 0;